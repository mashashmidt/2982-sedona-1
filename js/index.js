/*document.querySelector('.main-nav__toggle').addEventListener(
    'click',
    e => {
        const menu = document.querySelector('.site-menu');
        if (menu.classList.contains('site-menu--hidden')) {
            menu.classList.remove('site-menu--hidden');
        } else {
            menu.classList.add('site-menu--hidden');
        }
    }
);
*/